# Server

Express + TypeScript + Prisma (SQLite for local dev).

Quick commands:
- Install: `npm ci` (from repo root)
- Dev: `npm run dev --prefix ./server`
- Test: `npm test --prefix ./server`
- Build: `npm run build --prefix ./server`
- Migrate: `npm run prisma:migrate --prefix ./server`
